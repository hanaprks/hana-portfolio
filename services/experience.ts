import { prisma } from "@/lib/prisma";
import type { ExperienceInput } from "@/lib/validations/experience";

// Placeholder methods until the 'Experience' model is added to schema.prisma
export async function getExperiences() {
  return [];
}

export async function createExperience(data: ExperienceInput) {
  return null;
}
